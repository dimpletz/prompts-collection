{
  "name": "poetry-user",
  "description": "Poetry User is a plugin that detects whether the current workspace uses Poetry (via poetry.lock), injects context instructing the agent to prefer poetry commands, and automatically installs Poetry via pip if it is not already installed.",
  "version": "1.0.0",
  "author": { "name": "Dimpletz" },
  "repository": "https://github.com/dimpletz/prompts-collection",
  "license": "MIT",
  "keywords": ["poetry", "python", "dependency-management", "hooks", "session"],
  "skills": [
    "./skills/vscode-poetry-configurator"
  ]
}
